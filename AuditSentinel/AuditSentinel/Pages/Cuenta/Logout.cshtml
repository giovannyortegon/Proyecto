@page
@model AuditSentinel.Pages.Cuenta.LogoutModel
@{
    ViewData["Title"] = "Cerrar Sesión";
}

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-lg border-0 rounded-4 overflow-hidden" style="max-width: 400px; width: 100%;">
        <div class="bg-light p-4 text-center border-bottom">
            <div class="feature-icon bg-danger bg-gradient text-white mb-3 mx-auto rounded-circle d-flex align-items-center justify-content-center shadow-sm" style="width: 70px; height: 70px;">
                <i class="bi bi-box-arrow-right fs-2"></i>
            </div>
            <h2 class="h4 fw-bold text-dark mb-1">@ViewData["Title"]</h2>
            <p class="text-muted small mb-0">Protección de Datos Activa</p>
        </div>

        <div class="card-body p-4">
            <p class="text-center text-secondary mb-4">
                ¿Estás seguro de que deseas finalizar tu sesión en <strong>AuditSentinel</strong>?
                Se cerrará el acceso a los módulos de auditoría.
            </p>

            <form method="post">
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-danger btn-lg shadow-sm fw-bold">
                        <i class="bi bi-check-circle me-2"></i>Sí, Cerrar Sesión
                    </button>
                    <a class="btn btn-outline-secondary py-2" asp-page="/Index">
                        <i class="bi bi-x-circle me-2"></i>Cancelar
                    </a>
                </div>
            </form>
        </div>

        <div class="card-footer bg-white border-top-0 py-3 text-center">
            <small class="text-muted">
                <i class="bi bi-info-circle me-1"></i> Recuerda cerrar tu navegador por seguridad.
            </small>
        </div>
    </div>
</div>

<style>
    body {
        background-color: #f8f9fc;
        background-image: radial-gradient(circle at 10% 20%, rgba(220, 53, 69, 0.05) 0%, transparent 90%);
    }

    .feature-icon {
        display: inline-flex;
    }

    .btn-danger {
        transition: all 0.3s ease;
    }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 .5rem 1rem rgba(220, 53, 69, 0.2) !important;
        }
</style>