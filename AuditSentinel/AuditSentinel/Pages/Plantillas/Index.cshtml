@page
@model AuditSentinel.Pages.Plantillas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Plantillas[0].NombrePlantilla)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plantillas[0].Version)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Plantillas) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombrePlantilla)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Version)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdPlantilla">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdPlantilla">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdPlantilla">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
