@page "{id:int}"
@model AuditSentinel.Pages.Plantillas.DeleteModel
@{
    ViewData["Title"] = "Eliminar Plantilla";
}

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-lg border-0 rounded-4 overflow-hidden" style="max-width: 500px; width: 100%;">

        <div class="bg-danger p-4 text-center text-white bg-gradient">
            <div class="mb-3">
                <i class="bi bi-file-earmark-x-fill shadow-sm" style="font-size: 3.5rem;"></i>
            </div>
            <h2 class="h4 fw-bold mb-0">¿Eliminar Plantilla?</h2>
            <p class="small opacity-75 mb-0 text-uppercase tracking-wider">Esta acción es irreversible</p>
        </div>

        <div class="card-body p-4 text-center">
            <p class="text-muted mb-4">
                Estás a punto de borrar permanentemente la configuración de auditoría:
                <br>
                <span class="h5 fw-bold text-dark d-block mt-2">
                    <i class="bi bi-file-earmark-medical text-primary me-2"></i>@Model.Plantilla.NombrePlantilla
                </span>
            </p>

            <div class="alert alert-warning border-0 small text-start mb-4">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <strong>Atención:</strong> Si eliminas esta plantilla, los escaneos configurados para usarla podrían quedar incompletos o requerir una nueva configuración manual.
            </div>

            <form method="post">
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-danger btn-lg shadow-sm fw-bold py-2">
                        <i class="bi bi-trash3 me-2"></i>Confirmar Eliminación
                    </button>
                    <a asp-page="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-x-circle me-2"></i>Cancelar y Volver
                    </a>
                </div>
            </form>
        </div>

        <div class="card-footer bg-light border-top-0 py-3 text-center">
            <small class="text-muted small">
                <i class="bi bi-shield-lock me-1"></i> AuditSentinel Security System
            </small>
        </div>
    </div>
</div>

<style>
    /* Estilo de fondo coherente con el módulo de seguridad */
    body {
        background-color: #f8f9fc;
        background-image: radial-gradient(circle at center, rgba(220, 53, 69, 0.04) 0%, transparent 80%);
    }

    /* Animación de entrada suave */
    .card {
        animation: zoomIn 0.3s ease-out;
    }

    @@keyframes zoomIn {
        from {
            opacity: 0;
            transform: scale(0.95);
        }

        to {
            opacity: 1;
            transform: scale(1);
        }
    }

    .btn-danger {
        transition: all 0.2s ease-in-out;
    }

        .btn-danger:hover {
            background-color: #bb2d3b;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3) !important;
        }
</style>