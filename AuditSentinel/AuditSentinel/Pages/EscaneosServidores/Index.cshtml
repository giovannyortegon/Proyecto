@page
@model AuditSentinel.Pages.EscaneosServidores.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EscaneosServidores[0].Servidores)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EscaneosServidores[0].Escaneos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EscaneosServidores) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Servidores.IP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Escaneos.NombreEscaneo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
