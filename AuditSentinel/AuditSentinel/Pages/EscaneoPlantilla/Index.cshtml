@page
@model AuditSentinel.Pages.EscaneoPlantilla.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EscaneosPlantillas[0].Escaneos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EscaneosPlantillas[0].Plantillas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EscaneosPlantillas) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Escaneos.NombreEscaneo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plantillas.NombrePlantilla)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
